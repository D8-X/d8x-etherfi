version: '3'
services:
  drip:
    build:
      context: .
      dockerfile: ./cmd/Dockerfile
    restart: always
    ports:
      # Default svc port is 8001
      - 8001:8001
    environment:
      DB_DSN: "${DB_DSN1}"
      CONFIG_PATH: /config_path
    logging:
      options:
        max-size: "10m"
        max-file: "10"
    configs:
      - config_path

configs:
  config_path:
    file: ./config/config.json